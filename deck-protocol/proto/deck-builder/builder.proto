syntax = "proto3";

package deck.builder;

option java_package = "org.deck.builder";
option java_outer_classname = "DeckBuilder";

import "deck-core/core.proto";

// Example
service Builder {
    // Example
    rpc BuildPackage(deck.core.Manifest) returns (deck.core.BuildResult);
}
