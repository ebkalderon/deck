syntax = "proto3";

package deck.daemon;

option java_package = "org.deck.daemon";
option java_outer_classname = "DeckDaemon";

import "deck-core/core.proto";

// Example
service Daemon {
    // Example
    rpc BuildPackage(deck.core.Manifest) returns (deck.core.BuildResult);
    // Example
    rpc GetBuildLog(deck.core.BuildLogRequest) returns (deck.core.BuildLogResponse);
}
